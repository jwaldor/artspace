{
    "name": "art",
    "private": true,
    "type": "module",
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "build": "npm run build --workspaces",
      "dev": "concurrently \"npm run dev --workspace art\" \"npm run dev --workspace backend\""
    }
  }